"""{{ app_name }} Django app configuration."""
from django.apps import AppConfig


class {{ app_name_capitalized }}Config(AppConfig):
    """Django app configuration for {{ app_name }}."""

    name = "{{ app_name }}"
    verbose_name = "{{ app_name_capitalized }}"