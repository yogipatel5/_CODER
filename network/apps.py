"""Network Django app configuration."""

from django.apps import AppConfig


class NetworkConfig(AppConfig):
    """Network app configuration."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "network"
